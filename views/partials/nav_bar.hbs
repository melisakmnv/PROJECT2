<div id='nav_bar_box'>
  <header class='main_head'>
    <img id="logo-nav" src="/images/temporary-logo.png" alt="Neotrip logo">
    {{> search_bar}}
    <nav class='nav_bar'>
      <a class='link_item' href='/'>Home</a>
      <a class='link_item' href='/destinations'>Destinations</a>
      <a class='link_item' href='/aboutus'>Who are we</a>

      {{#if isLoggedIn}}
        <a class='link_item' href='/dashboard'> My Dashboard</a>
      {{/if}}

      <div>
        {{#unless isLoggedIn}}
          <a class='link_item' href='/auth/signin'>Sign in</a>
        {{/unless}}
      </div>

      {{#if isLoggedIn}}
        <a class='link_item' href='/auth/logout'>Sign out</a>
      {{/if}}
    </nav>
  </header>
</div>

